
function chooseWay(event){
  const buttons= document.querySelectorAll(".radio-button");
 
  buttons.forEach(button=>{
    if(button == event.target){
        button.classList.add('active')
    }
    else{
        button.classList.remove('active')
    }
  })
  if(value == )
}